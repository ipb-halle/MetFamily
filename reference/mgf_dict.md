# MGF header dictionnary

Name matching for stadardized metadata. Standard names based on
`Spectra::spectraVariableMapping(MsBackendMsp::MsBackendMsp())`

## Usage

``` r
mgf_dict()
```

## Value

named vector with standardized names as value
